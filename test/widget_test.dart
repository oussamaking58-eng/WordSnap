import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Lancement de LingoSnap', (WidgetTester tester) async {
    // Ce test vérifie juste que l'app ne plante pas au démarrage
    // Mais comme tu as Firebase, ce test risque d'échouer sans configuration spéciale.
  });
}
